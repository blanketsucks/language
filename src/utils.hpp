#ifndef _UTILS_HPP
#define _UTILS_HPP

#include <iostream>

#define TODO(x) std::cout << "TODO: " << "(" << __FILE__ << ":" << __LINE__ << ") " << x << '\n'; exit(1)
#define UNUSED(x) (void)x

#endif